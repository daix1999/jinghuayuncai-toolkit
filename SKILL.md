---
name: jinghuayuncai-toolkit
slug: jinghuayuncai-toolkit
displayName: 京华云采工具包
version: 1.1.0
summary: 获取京华云采经销商联系方式与销售数据：名单补电话 + 按产品/品类/品牌查经销商 + 拉销售成交记录
description: 京华云采经销商数据查询工具。三大能力：(1) 名单补电话——用户提供一个京华云采供应商/经销商名单（xlsx），补全其中联系电话为空的联系方式，先走官网 API 抓包查询（querySkuAgentListFromEs 直接返回 agentPhone），未命中的再公开搜索，并标注电话来源和联系人身份；(2) 查经销商——按指定产品、品类或品牌，爬取京华云采上对应的所有经销商及联系方式，统一输出格式"品类、品牌、产品、经销商名称、电话"；(3) 销售记录——按型号、品牌或品类拉取历史成交数据，按型号、采购单位、采购数量、采购单价、供应商、成交时间建表。适用关键词：京华云采、查经销商、获取联系方式、补电话、填充电话、销售记录、成交记录、采购单位、供应商名单、经销商名单、按品牌查经销商、按品类查经销商、联想经销商。
tags: [京华云采, 经销商, 联系方式, 电话, 销售记录, 成交数据, 渠道拓展, 政府采购, 供应商, 办公效率]
license: MIT
agent_created: true
---

# 京华云采工具包

京华云采（北京市政府采购平台）经销商数据与销售数据工具包：名单补电话、按维度圈经销商、拉销售成交记录。

## 三大能力

### 能力一：名单电话填充（反向匹配）

给定一个供应商名单，查出这些供应商的电话。

### 能力二：经销商爬取（正向维度）

按产品 / 品类 / 品牌，爬取对应的所有经销商及联系方式。

### 能力三：销售记录获取（成交分析）

按型号 / 品牌 / 品类，拉取历史销售成交记录，输出"型号、采购单位、采购数量、采购单价、供应商、成交时间"表。

---

## 能力一：名单电话填充

### 触发场景

- 用户提供一个京华云采供应商名单（xlsx），指出电话列有空白
- 要求"填充电话""补全联系方式""把空号补上"
- 名单可能是"云采代翔""近两年下单经销商"或其他云采供应商清单

### 工作流

**第一步：读懂名单，找空号**

1. 用 pandas 读取 xlsx，确认列结构（供应商名称列、电话列、备注列的确切位置）
2. 警惕多行表头：京华云采名单常有标题行 + 子表头行，数据可能从第 3 行才真正开始
3. 电话为空判断：`isna()` 或值为空字符串 / `'nan'` / `'None'`
4. 提取空号公司的名单，存成 JSON 数组 `["公司A", "公司B", ...]`

**第二步：通过京华云采官网 API 查询（零成本，最优先）**

用 `scripts/query_suppliers_by_api.py` 抓包查询：

```bash
python scripts/query_suppliers_by_api.py <供应商名单.json> --categories 台,笔,服,印 --out api_match_result.json
```

- 脚本遍历各品类（台式/笔记本/服务器/打印机）全部商品的经销商列表，按 `agentName` 精确匹配名单
- API 端点与参数详见 `references/api_docs.md`（核心：`querySkuAgentListFromEs` 直接返回 `agentPhone`）
- 命中 → 电话来源标注「京华云采API」，绿色
- 未命中 → 进入第三步公开搜索

**第三步：公开搜索（对 API 未命中的公司）**

未命中公司较多时（≥5 家），优先调用 `riskbird-cominfo-batch` 技能走风鸟批量查询；零星几家再用 WebSearch 逐家搜。渠道与技巧详见 `references/search_tips.md`，核心要点：

- **批量优先（风鸟）**：调用 `riskbird-cominfo-batch` 技能，通过风鸟（riskbird.com）CDP 浏览器自动化批量提取电话/邮箱/地址/法人/注册资本，无需付费、无需 API Key。前置条件：用户 Chrome 已打开风鸟、CDP Proxy 运行（localhost:3456）、名单为 JSON 数组
- **零星补漏（WebSearch）**：搜索词 `"公司全名" 电话 联系方式`、`"公司全名" 中标 OR 成交 供应商`
- 优先渠道：风鸟（批量）→ 黄页类 → 企业征信（企查查/天眼查/启信宝）→ 政府采购网（能拿真实对接人）→ 官网
- 每条结果提取：电话号码 + 法人/联系人 + 注册资本（用于身份备注）
- 号码打码时用多来源交叉验证；无法确认时标注"未找到"，绝不填猜测号码

**第四步：填充副本（不覆盖原文件）**

合并 API 结果和公开搜索结果，构造映射 JSON：
```json
{"公司名": {"phone": "号码", "source": "京华云采API 或 公开搜索-天眼查", "contact": "法人:XXX, 注册资本XXX万"}}
```
再用 `scripts/fill_phones.py` 填充：

```bash
python scripts/fill_phones.py <输入xlsx> <映射json> --name-col A --phone-col D --remark-col F
```

- 默认输出到 `原名_已填充.xlsx`，原文件不动
- 脚本自动新增两列：`电话来源` 和 `可能联系人及身份`
- 颜色标注：绿色 = 京华云采API，黄色 = 公开搜索，橙色 = 未找到

**第五步：交付并说明**

- 用 present_files 展示填充后的副本
- 汇总：共空号 N 家，填充 M 家，未找到 K 家
- 点出高价值供应商（本身就在做联想、注册资本大、是政采定点供应商等）

---

## 能力二：经销商爬取（正向维度）

### 触发场景

- "查一下京华云采上联想所有产品的经销商"
- "台式机品类下都有哪些经销商"
- "开天M70d 这款产品有哪些经销商在卖"
- 想按品牌/品类/产品维度圈定一批经销商，做渠道拓展

### 工作流

用 `scripts/crawl_dealers.py` 正向爬取：

```bash
# 指定品类下所有产品的经销商
python scripts/crawl_dealers.py --category 台 --out 台式机经销商.xlsx

# 指定品牌下所有产品的经销商
python scripts/crawl_dealers.py --brand 联想 --out 联想经销商.xlsx

# 指定产品（关键词）的经销商
python scripts/crawl_dealers.py --product 开天M70d --out 开天M70d经销商.xlsx

# 组合：台式品类 + 联想品牌
python scripts/crawl_dealers.py --category 台 --brand 联想 --out 台式联想经销商.xlsx
```

- `--category`：品类键（台/笔/服/印），逗号分隔，默认全部
- `--brand`：品牌名（如 联想），模糊包含匹配，逗号分隔
- `--product`：产品关键词（如 开天M70d、X5z），按 skuName 包含匹配
- 三个维度可任意组合，也可单独使用

**统一输出格式**（无论哪种维度，都输出这张表）：

| 品类 | 品牌 | 产品 | 经销商名称 | 销售电话 |
|------|------|------|-----------|---------|

- 同一经销商代理多款产品时，自动聚合到一行（品类/品牌/产品用"、"分隔，电话用" / "分隔）
- 输出 Excel，直接可用于渠道拓展跟进

---

## 能力三：销售记录获取（成交分析）

### 触发场景

- "查一下联想开天M70d 的销售记录"
- "台式机品类最近成交了多少、谁采购的"
- "某型号在政府采购里卖了多少台、什么价"
- 想了解某品牌/型号的真实成交情况（采购单位、数量、单价），做竞品分析和报价参考

### 工作流

用 `scripts/fetch_sale_records.py` 拉取：

```bash
# 指定型号的销售记录
python scripts/fetch_sale_records.py --product 开天M70d --out 开天M70d销售记录.xlsx

# 指定品牌的销售记录
python scripts/fetch_sale_records.py --brand 联想 --out 联想销售记录.xlsx

# 指定品类的销售记录
python scripts/fetch_sale_records.py --category 台 --out 台式机销售记录.xlsx
```

- 先通过商品搜索接口拿到目标商品的 `skuId` 列表
- 再对每个 `skuId` 调销售记录接口（`shop-bjzc.zhongcy.com` 的 `querySkuSaleRecord`，GET 方法）
- 汇总输出统一表

**统一输出格式**：

| 品类 | 品牌 | 型号 | 采购单位 | 采购数量 | 采购单价(元) | 供应商 | 成交时间 |
|------|------|------|---------|---------|-------------|--------|---------|

- 销售记录接口字段名已实测对齐：`organizeName`(采购单位)、`shopName`(供应商)、`skuNum`(数量)、`sellPrice`(单价)、`orderTime`(成交时间)，结果数组在 `data.result`，分页用 `data.totalPageCount`

---

## 关键经验（务必遵守）

1. **列号要对**：供应商名称可能在 A 列或 B 列（0729名单在A列，0723名单在B列），填充前先确认
2. **多行表头**：跳过标题行和子表头行，从真正数据行开始
3. **副本操作**：只输出 `_已填充.xlsx`，绝不覆盖原始文件
4. **来源必标**：每个电话都标注来源，便于用户判断可信度
5. **联系人备注**：身份信息（法人/对接人/注册资本）写进独立列，是后续沟通的弹药
6. **不猜号码**：打码号码无法确认时标"未找到"，错号比空号更伤信任
7. **API 限频**：请求间 sleep 0.15~0.3 秒，pageSize 用 100（超限报错）
8. **打印机 cid**：打印机有 5 个子类（1000023/25/27/29/41），脚本已内置展开
